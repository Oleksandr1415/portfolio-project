---
import { defaultLang } from '@/i18n/ui';
import { getRelativeLocaleUrl } from 'astro:i18n';

const lang = Astro.request.headers.get('accept-language') || defaultLang;

return Astro.redirect(getRelativeLocaleUrl(lang, ''), 301);
---
